Problem 2:

// input A: an array of sorted distinct integers
// output integer: an integer such that A[integer] = integer

PROCEDURE findMatch(A):
	
		lo <- 0
		hi <- A.length-1
		
		if lo > hi: 
			return "no solution"
		
		mid <- hi - (hi - lo)/2
		
		if A[mid] == mid:
			return mid
			
		else if A[mid] > mid:
			return findMatch(A[lo...mid])
		
		else
			return findMatch(A[mid+1...hi])
			
END PROCEDURE

rewrite but passing in lo and hi

		
		
		
